// src/config.ts

export const siteConfig = {
  name: "SAU EconHub",
  description: "Blogs & Question Bank for Agricultural Economics students at SAU.",
  url: "https://your-domain.com",
};

/**
 * Toggle this to false when selling/licensing to another university
 * so the SAU-specific gate and branding disappears automatically.
 */
export const SHOW_SAU_GATE = true;